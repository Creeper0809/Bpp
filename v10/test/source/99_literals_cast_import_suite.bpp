// Covers: grouped runtime sanity tests (literals/cast/import)

//=== CASE 99_char_literal_const_basic
// Covers: char literals in const declarations
// Expect exit code: 0

const A = 'A';
const NL = '\n';

func main() -> u64 {
    if (A != 65) { return 1; }
    if (NL != 10) { return 2; }
    return 0;
}
//=== END

//=== CASE 100_cast_f64_basic
// Covers: explicit cast to f64 in expression position
// Expect exit code: 0

func main() -> u64 {
    var x: u64 = 7;
    var y: f64 = (f64)x;
    if (y == y) { return 0; }
    return 1;
}
//=== END

//=== CASE 106_import_std_char_keyword_module
// Covers: import path should still allow std.char module name
// Expect exit code: 0

import std.char;

func main() -> u64 {
    return 0;
}
//=== END

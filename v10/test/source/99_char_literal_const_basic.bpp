// Covers: char literals in const declarations
// Expect exit code: 0

const A = 'A';
const NL = '\n';

func main() -> u64 {
    if (A != 65) { return 1; }
    if (NL != 10) { return 2; }
    return 0;
}

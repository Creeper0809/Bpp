// Covers: compile-fail check for trait impl self type mismatch validation
// Expect compile fail: 1
// Expect error contains: Trait impl self type mismatch

trait ShapeLike {
    func area(self: *Self) -> i64;
}

struct Circle {
    public r: i64;
}

struct Square {
    public w: i64;
}

impl ShapeLike for Circle {
    public func area(self: *Square) -> i64 {
        return self.w * self.w;
    }
}

func main() -> i64 {
    return 0;
}

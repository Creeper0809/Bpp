// Covers: default-private behavior for struct fields without explicit modifier
// Expect compile fail: 1
// Expect error contains: private field access denied

struct AutoPrivateField {
    value: i64;
}

func main() -> i64 {
    var x: AutoPrivateField;
    x.value = 7;
    return 0;
}

// Covers: debug keywords assert/todo/unreachable
// Expect exit code: 0

func main(argc, argv) -> i64 {
    // Import

    // Setup
    var x: i64 = 3;

    // Execution
    assert(x == 3);
    if (0) {
        todo;
    }
    if (0) {
        unreachable;
    }

    // Assertion
    return 0;
}

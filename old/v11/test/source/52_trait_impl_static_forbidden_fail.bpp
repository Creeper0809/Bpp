// Covers: compile-fail check for forbidden static methods in impl Trait for Type blocks
// Expect compile fail: 1
// Expect error contains: trait impl methods cannot be static

trait Ops {
    func run(self: *Self) -> i64;
}

struct Runner {
    public v: i64;
}

impl Ops for Runner {
    public static func run(self: *Runner) -> i64 {
        return self.v;
    }
}

func main() -> i64 {
    return 0;
}

// Covers: compile-fail check for abstract+static invalid combination
// Expect compile fail: 1
// Expect error contains: abstract method cannot be static

struct BadCombo {
    v: i64;
}

impl BadCombo {
    static abst func f(self: *BadCombo) -> i64;
}

func main() -> i64 {
    return 0;
}

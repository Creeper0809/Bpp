// Covers: compile-fail for super usage outside impl method context
// Expect compile fail: 1
// Expect error contains: 'super' is only allowed inside impl methods

struct X {
    public v: i64;
}

func main() -> i64 {
    var x: X;
    x.v = 1;
    return super.v;
}

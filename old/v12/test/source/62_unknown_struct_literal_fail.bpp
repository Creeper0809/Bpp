// Covers: unknown struct literal should fail during parsing (no null struct_def in AST)
// Expect compile fail: 1
// Expect error contains: Unknown struct/union type for literal

func main() -> i64 {
    var v = MissingType{1, 2};
    return v;
}
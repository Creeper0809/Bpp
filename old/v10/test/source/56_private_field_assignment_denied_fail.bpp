// Covers: compile-fail check for private field assignment from outside owner struct (lvalue path)
// Expect compile fail: 1
// Expect error contains: private field access denied

struct HiddenWrite {
    private v: i64;
}

impl HiddenWrite {
    public func set_inside(self: *HiddenWrite, x: i64) {
        self.v = x;
    }
}

func main() -> i64 {
    var h: HiddenWrite;
    h.set_inside(1);
    h.v = 9;
    return 0;
}

// Covers: static generic method call via imported module struct type
// Expect exit code: 0

import modules.mod_static_generic_method;

func main() -> i64 {
    var a: i64 = StaticGenericBox.pick<i64>(7);
    var b: u64 = StaticGenericBox.pick<u64>(5);
    if (a != 7) { return 1; }
    if (b != 5) { return 2; }
    return 0;
}

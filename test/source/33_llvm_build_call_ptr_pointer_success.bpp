// LLVM Build: true
// Mode: ssa

@[feature]
func write_and_sum_113(p: *u8, v: u8) -> u64 {
    *p = v;
    return 0;
}

@[entry]
func runtime_entry() -> u64 {
    var buf: [2]u8;
    buf[0] = 1;
    buf[1] = 2;
    var p: *u8 = &buf[1];
    var fp: u64 = &write_and_sum_113;
    fp(p, (u8)44);
    if (buf[1] != 44) { return 1; }
    return 0;
}

enum ExprMatchEnum {
    None,
    Some(value: u64),
}

func main() -> u64 {
    var out: u64 = match (ExprMatchEnum.Some(42)) {
        case ExprMatchEnum.None:
            0;
        case ExprMatchEnum.Some(x):
            x;
    };
    if (out != 42) { return 1; }
    return 0;
}

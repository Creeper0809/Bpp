// Expect compile fail: 1
// Expect error contains: return type mismatch

struct MatchStructFail {
    public x: u64;
}

func bad_match_ret() -> MatchStructFail {
    return match (1) {
        case 1:
            1;
        case _:
            2;
    };
}

func main() -> u64 {
    var v: MatchStructFail = bad_match_ret();
    return v.x;
}

func main() -> u64 {
    var base: u64 = 40;
    var x: u64 = 2;
    var y: u64 = match (x) {
        case 1:
            base + 1;
        case 2, 3:
            base + x;
        case _:
            0;
    };

    if (y != 42) { return 1; }
    return 0;
}

// Covers: number rejects ordered comparisons for non-real complex values
// Mode: ssa|nossa
// Opt: O0|O1
// LLVM Build: true
// Expect exit code: 1

func main() -> u64 {
    var z: number = 1 + 2i;
    var w: number = 3 + 4i;
    if (z < w) {
        return 42;
    }
    return 0;
}

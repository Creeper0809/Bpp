struct CalcEx {
}

impl CalcEx {
    public static func add(a: u64, b: u64 = 40) -> u64 {
        return a + b;
    }
}

func main() -> u64 {
    var v0: u64 = CalcEx.add(2);
    var v1: u64 = CalcEx.add(b: 39, a: 3);
    if (v0 != 42) { return 1; }
    if (v1 != 42) { return 2; }
    return 0;
}

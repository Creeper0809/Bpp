// Covers: backend selection is independent of the earlier `-dump-ssa` mode
// flag inserted by the test runner.
// Mode: ssa|nossa
// Opt: O0|O1
// Compiler args: --backend legacy
// Expect exit code: 0

@[hot]
func legacy_only_helper89() -> u64 {
    return 89;
}

func main() -> u64 {
    if (legacy_only_helper89() != 89) { return 1; }
    return 0;
}

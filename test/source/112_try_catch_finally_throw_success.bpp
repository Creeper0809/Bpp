func main() -> u64 {
    var x: u64 = 0;
    try {
        x = x + 1;
        throw 123;
        x = x + 100;
    } catch {
        x = x + 10;
    } finally {
        x = x + 1000;
    }

    if (x != 1011) { return 1; }
    return 0;
}

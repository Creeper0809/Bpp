// Expect compile fail: 1
// Expect error contains: catch binding requires explicit payload type

func main() -> u64 {
    try {
        throw 1;
    } catch (e) {
        return e;
    }
    return 0;
}

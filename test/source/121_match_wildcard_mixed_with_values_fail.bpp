// Expect compile fail: 1
// Expect error contains: wildcard '_' cannot be mixed with explicit case values

func main() -> u64 {
    match (1) {
        case 1, _:
            return 1;
        }
    }
    return 0;
}

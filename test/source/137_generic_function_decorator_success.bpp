func plus_one(next: u64, x: u64) -> u64 {
    return next(x) + 1;
}

@[plus_one]
func decorated_twice<T>(x: T) -> T {
    return x + x;
}

func main() -> u64 {
    var v: u64 = decorated_twice<u64>(20);
    if (v != 41) { return 1; }
    return 0;
}

// Covers: static generic method call must resolve owner struct type during lowering
// Expect compile fail: 1
// Expect error contains: static generic method owner type not found

func main() -> i64 {
    return NotAType.pick<i64>(1);
}

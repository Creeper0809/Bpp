// Covers: an explicitly selected SSA backend must diagnose instead of falling
// back to legacy code generation for any function.
// Mode: ssa|nossa
// Opt: O0|O1
// Compiler args: --backend ssa
// Expect compile fail: 1
// Expect error contains: strict SSA backend

func main() -> u64 {
    return 0;
}

// Expect compile fail: 1
// Expect error contains: complexity mode mismatch on function

@[complexity(time="O(n)", space="O(1)", mode="worst")]
@[complexity_budget(time="O(n)", space="O(1)", mode="expected")]
@[constraints(n=1024)]
func mismatch_mode(n: u64) -> u64 {
    var i: u64 = 0;
    while (i < n) {
        i += 1;
    }
    return i;
}

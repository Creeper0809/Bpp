// Expect compile fail: 1
// Expect error contains: complexity_input key appears unused

@[complexity_input(n="len(arr)")]
@[complexity_budget(time="O(m)", space="O(1)")]
@[constraints(m=1024)]
@[complexity_debug(warnings_as_errors=true)]
func complexity_input_warnings_as_errors_fail(arr_n: u64, m: u64) -> u64 {
    var i: u64 = 0;
    var s: u64 = 0;
    while (i < m) {
        s += (i & 1);
        i += 1;
    }
    return s + arr_n;
}

func main() -> u64 {
    return complexity_input_warnings_as_errors_fail(8, 16);
}

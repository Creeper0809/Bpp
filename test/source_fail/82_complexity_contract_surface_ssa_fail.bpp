// Covers: complexity contract violation introduced on decorated public SSA surface
// Expect compile fail: 1
// Mode: ssa
// Opt: O1
// Expect error contains: complexity contract not preserved after SSA/codegen

@[complexity(time="O(n)", space="O(1)")]
func deco_linear_surface_fail_82(next: u64, n: u64) -> u64 {
    var i: u64 = 0;
    var acc: u64 = 0;
    while (i < n) {
        acc = acc + 1;
        i = i + 1;
    }
    return next(n) + acc;
}

@[deco_linear_surface_fail_82]
@[complexity(time="O(1)", space="O(1)")]
@[complexity_debug]
func decorated_constant_surface_fail_82(n: u64) -> u64 {
    return 1;
}

func main() -> u64 {
    return decorated_constant_surface_fail_82(8);
}

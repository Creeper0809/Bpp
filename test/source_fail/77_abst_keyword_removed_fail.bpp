// Covers: legacy abstract shorthand should fail with migration guidance
// Expect compile fail: 1
// Expect error contains: 'abst' has been removed; use 'abstract'

struct OldAbstractKeyword {
    public value: i64;
}

impl OldAbstractKeyword {
    public abst func calc(self: *OldAbstractKeyword) -> i64;
}

// Expect compile fail: 1
// Expect error contains: complexity budget violated on function

@[complexity(time="O(n!*n)", space="O(1)")]
@[complexity_budget(time="O(n!)", space="O(1)")]
@[constraints(n=12)]
func factorial_mul_budget_fail(n: u64) -> u64 {
    var i: u64 = 0;
    var acc: u64 = 0;
    while (i < n) {
        acc += i;
        i += 1;
    }
    return acc;
}

func main() -> u64 {
    return factorial_mul_budget_fail(8);
}

// Expect compile fail: 1
// Expect error contains: complexity confidence UNKNOWN on function

@[complexity_budget(time="O(n)", space="O(1)")]
@[constraints(n=2048)]
func unknown_confidence_budget_fail(n: u64) -> u64 {
    var i: u64 = 0;
    var s: u64 = 0;
    while ((i + 1) < n) {
        s += (i & 1);
        i += 1;
    }
    return s;
}

func main() -> u64 {
    return unknown_confidence_budget_fail(64);
}

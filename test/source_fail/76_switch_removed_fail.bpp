// Covers: legacy switch keyword should fail with migration guidance
// Expect compile fail: 1
// Expect error contains: 'switch' has been removed from the surface language

func main() -> i64 {
    switch (1) {
        case 1:
            return 0;
        default:
            return 1;
    }
}

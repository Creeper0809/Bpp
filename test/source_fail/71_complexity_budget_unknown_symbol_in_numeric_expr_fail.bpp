// Expect compile fail: 1
// Expect error contains: unsupported time expression for numeric evaluation

@[complexity_budget(time="O(max(n,x))", space="O(1)")]
@[constraints(n=256)]
func complexity_budget_unknown_symbol_in_numeric_expr_fail(n: u64) -> u64 {
    var i: u64 = 0;
    var s: u64 = 0;
    while (i < n) {
        s += i;
        i += 1;
    }
    return s;
}

func main() -> u64 {
    return complexity_budget_unknown_symbol_in_numeric_expr_fail(16);
}

// Expect compile fail: 1
// Expect error contains: duplicate complexity_input key is not allowed

@[complexity_input(n="len(arr)", n="len(vals)")]
func complexity_input_duplicate_key_fail(arr_n: u64) -> u64 {
    var i: u64 = 0;
    var s: u64 = 0;
    while (i < arr_n) {
        s += i;
        i += 1;
    }
    return s;
}

func main() -> u64 {
    return complexity_input_duplicate_key_fail(8);
}

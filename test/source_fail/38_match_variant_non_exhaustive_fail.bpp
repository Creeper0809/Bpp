// Expect compile fail: 1
// Expect error contains: member: ColorX.Blue

enum ColorX {
    Red,
    Blue,
}

func main() -> u64 {
    var c: ColorX = ColorX.Red();
    match (c) {
        case ColorX.Red:
            return 1;
    }
    return 0;
}

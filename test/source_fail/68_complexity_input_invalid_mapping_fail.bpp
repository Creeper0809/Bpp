// Expect compile fail: 1
// Expect error contains: input mapping values must be identifier or len(x)/size(x)/length(x)

@[complexity_input(n="arr+1")]
func complexity_input_invalid_mapping_fail(arr_n: u64) -> u64 {
    var i: u64 = 0;
    var s: u64 = 0;
    while (i < arr_n) {
        s += i;
        i += 1;
    }
    return s;
}

func main() -> u64 {
    return complexity_input_invalid_mapping_fail(8);
}

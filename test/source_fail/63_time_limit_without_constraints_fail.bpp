// Expect compile fail: 1
// Expect error contains: requires @constraints(...) for bounded static estimation

@[complexity(time="O(n)", space="O(1)")]
@[time_limit(ms=10)]
func time_limit_without_constraints_fail(n: u64) -> u64 {
    var i: u64 = 0;
    var s: u64 = 0;
    while (i < n) {
        s += (i & 1);
        i += 1;
    }
    return s;
}

func main() -> u64 {
    return time_limit_without_constraints_fail(32);
}

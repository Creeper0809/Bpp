// .vscode/settings.json
{
    "files.associations": {
        "*.bpp": "rust",       // 혹은 "cpp" (클래스나 템플릿 문법이 필요하다면)
        "*.b": "c"           // 확장자가 여러 개라면 추가
    },
    // 2026년 트렌드: AI가 이 파일을 무시하지 않도록 명시 (선택사항)
    "search.exclude": {
        "**/*.bpp": false
    }
}
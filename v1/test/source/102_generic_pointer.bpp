// Covers: generic pointer parameter
// Mode: nossa
// Opt: O0
// Expect exit code: 0

func first<T>(p: *T) -> *T {
    return p;
}

func main() -> u64 {
    var x: u64 = 7;
    var p: *u64 = &x;
    var q: *u64 = first(p);
    if (*q != 7) { return 3; }
    return 0;
}

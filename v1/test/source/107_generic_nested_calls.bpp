// Covers: nested generic calls
// Mode: nossa
// Opt: O0
// Expect exit code: 0

func id<T>(x: T) -> T { return x; }
func add<T>(a: T, b: T) -> T { return a + b; }

func main() -> u64 {
    var v: u64 = add(id(7), id(8));
    if (v != 15) { return 8; }
    return 0;
}

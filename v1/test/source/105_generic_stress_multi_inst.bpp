// Covers: stress test for many generic instantiations
// Mode: nossa
// Opt: O0
// Expect exit code: 0

func id<T>(x: T) -> T { return x; }

func main() -> u64 {
    var a: u64 = id<u64>(1);
    var b: u16 = id<u16>(2);
    var c: u8 = id<u8>(3);
    var d: i64 = id<i64>(4);
    var e: u32 = id<u32>(5);
    if (a + b + c + d + e != 15) { return 6; }
    return 0;
}

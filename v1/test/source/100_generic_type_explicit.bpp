// Covers: generic function explicit type args
// Mode: nossa
// Opt: O0
// Expect exit code: 0

func id<T>(x: T) -> T {
    return x;
}

func main() -> u64 {
    var a: u64 = id<u64>(123);
    if (a != 123) { return 1; }
    return 0;
}

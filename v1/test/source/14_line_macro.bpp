// Covers: __LINE__ macro (edge + stress)
// Expect exit code: 0

func line_value() -> i64 {
    return __LINE__;
}

func main(argc, argv) -> i64 {
    // Import

    // Setup
    var l0: i64 = __LINE__;
    var l1: i64 = __LINE__;
    var l2: i64 = __LINE__;
    var sum: i64 = 0;

    // Execution
    if (l1 != l0 + 1) { return 1; }
    if (l2 != l1 + 1) { return 2; }

    // Stress: 동일 라인 값 반복 합산
    for (var i: i64 = 0; i < 1000; i = i + 1) {
        sum = sum + line_value();
    }
    if (sum != line_value() * 1000) { return 3; }

    // Assertion
    return 0;
}

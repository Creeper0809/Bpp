// Covers: multiple generic params with inference
// Mode: nossa
// Opt: O0
// Expect exit code: 0

func pick_first<T, U>(a: T, b: U) -> T {
    return a;
}

func main() -> u64 {
    var v: u64 = pick_first(9, 1);
    if (v != 9) { return 4; }
    return 0;
}

// Covers: generic type inference from arguments
// Mode: nossa
// Opt: O0
// Expect exit code: 0

func id<T>(x: T) -> T {
    return x;
}

func main() -> u64 {
    var x: u64 = 55;
    var y: u64 = id(x);
    if (y != 55) { return 2; }
    return 0;
}

// Covers: value generics used in array types
// Mode: nossa
// Opt: O0
// Expect exit code: 0

func size_of_buf<const N: u64>() -> u64 {
    return sizeof([N]u64);
}

func main() -> u64 {
    var size: u64 = size_of_buf<4>();
    if (size != 32) { return 5; }
    return 0;
}

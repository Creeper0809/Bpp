// Covers: generic inference under SSA
// Mode: ssa
// Opt: O0
// Expect exit code: 0

func id<T>(x: T) -> T {
    return x;
}

func main() -> u64 {
    // Setup
    var x: u64 = 77;

    // Execution
    var y: u64 = id(x);

    // Assertion
    if (y != 77) { return 10; }
    return 0;
}
